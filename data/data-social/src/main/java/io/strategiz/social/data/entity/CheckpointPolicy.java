package io.strategiz.social.data.entity;

/** Checkpoint policy controlling when agent execution pauses for user approval. */
public enum CheckpointPolicy {

	AUTO, CHECKPOINT_MAJOR, CHECKPOINT_ALL

}
