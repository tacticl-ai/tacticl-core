# 08 — Memory + Docs Index

Purpose: tell a fresh session exactly which memory files and canonical docs to read (and which to UPDATE) before resuming the orchestrator → arbiter migration. All paths absolute and verified by reading/listing the actual files. Architectural decisions are NOT repeated here — see the companion `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/handover/2026-05-30-orchestrator-migration/00-session-decisions.md`.

> Accuracy note: an earlier draft of this section invented filenames (e.g. "pdlc-pipeline-arbiter-migration.md", "temporal-workflow-orchestration-architecture.md") that do NOT exist. Those are corrected below. Every path here was verified against the filesystem; where a fact could not be re-verified it is marked **UNVERIFIED**.

---

## Part A — Auto-memory files (read first)

Directory: `/Users/cuztomizer/.claude/projects/-Users-cuztomizer-Documents-GitHub-tacticl-core/memory/`

Entry point / index: `MEMORY.md` in that directory (11933 bytes). The orchestrator/arbiter/pipeline-relevant entries, with the one-line hooks **as they literally appear in `MEMORY.md`** (verified line numbers via grep), are:

| File (absolute) | MEMORY.md hook (verbatim) | MEMORY.md line |
|---|---|---|
| `/Users/cuztomizer/.claude/projects/-Users-cuztomizer-Documents-GitHub-tacticl-core/memory/project_cloud_agent_orchestrator.md` | "Unified persona orchestrator on Temporal + live voice (Deepgram/ElevenLabs) + voice sphere UI. **Temporal deferral OVERRIDDEN** — adopting full Postgres-backed cluster now. PDLC v2 PRD/SAD superseded for orchestration." | 144 |
| `/Users/cuztomizer/.claude/projects/-Users-cuztomizer-Documents-GitHub-tacticl-core/memory/reference_cloud_agent_orchestrator_docs.md` | "Canonical PRD/SAD/plan paths." | 145 |
| `/Users/cuztomizer/.claude/projects/-Users-cuztomizer-Documents-GitHub-tacticl-core/memory/project_arbiter_integration.md` | "Tacticl ↔ arbiter gRPC integration, replaces direct LLM clients + CLI engines with centralized arbiter" | 132 |
| `/Users/cuztomizer/.claude/projects/-Users-cuztomizer-Documents-GitHub-tacticl-core/memory/pdlc_pipeline_engine.md` | "See [pdlc_pipeline_engine.md] for full implementation status and TODOs" | 126 |
| `/Users/cuztomizer/.claude/projects/-Users-cuztomizer-Documents-GitHub-tacticl-core/memory/project_conversational_spark_model.md` | "Telegram routes through ConversationService (gather → propose → active), not the spark-bypass path. Plan: docs/superpowers/plans/2026-05-15-telegram-conversational-spark-to-pdlc.md." | 138 |
| `/Users/cuztomizer/.claude/projects/-Users-cuztomizer-Documents-GitHub-tacticl-core/memory/project_multiple_pipelines.md` | "PDLC is first pipeline, more coming. UI should be pipeline-agnostic at container level." | 129 |
| `/Users/cuztomizer/.claude/projects/-Users-cuztomizer-Documents-GitHub-tacticl-core/memory/project_database_migration.md` | "**Tacticl should be fully on MongoDB (Hetzner), NOT Firestore** — migration is unfinished" | 19 |
| `/Users/cuztomizer/.claude/projects/-Users-cuztomizer-Documents-GitHub-tacticl-core/memory/project_voice_provider_recommendations.md` | "When implementing voice: **Deepgram** for STT, **ElevenLabs** for TTS." | 141 |

What each currently SAYS (verified by reading the file, so you know its state before deciding whether to trust it):

- **project_cloud_agent_orchestrator.md** (14626 bytes, modified 2026-05-30 11:02) — The richest file. Covers the 2026-05-25 pivot (persona orchestrator on Temporal, voice plane, sphere UI) AND already contains a **"Multi-session + multi-pipeline (user-scoped) — 2026-05-30 refinement"** section (lines 18-37): pipelines are user-scoped not session-scoped; `PipelineWorkflow` started `ParentClosePolicy.ABANDON`; `WorkflowExecutionTimeout = 7 days`; `PipelineRun.userId` primary query axis; new `PipelineRun.name` field; Mongo `pipeline_runs`/`pipeline_events`/`pipeline_checkpoints` are projections (Temporal history is source of truth); `loadUserActivePipelines(userId)` activity; checkpoint fan-out + optimistic-locked resolution; new skills `list_user_pipelines`, `cancel_pipeline`. Also: tear-out list (lines 69-74), PM→PO rename (lines 93-97), two persona execution paths (CONVERSATIONAL in-JVM vs PDLC ephemeral container via Arbiter, lines 111-120), voice plane decisions, sphere UI, module additions, single-deploy cutover/no-feature-flags (lines 151-161). **Critically: it still frames the whole orchestrator + persona registries as living in tacticl-core (Java). It does NOT reflect the 2026-05-30 "Option B" decision that the orchestrator + pipeline ENGINE moves into `cidadel-ai-arbiter` (TypeScript).** This is the single biggest drift risk.
- **reference_cloud_agent_orchestrator_docs.md** (1785 bytes, 2026-05-26) — Points to the 2026-05-25 PRD/SAD/plan as canonical; lists `2026-04-11-tacticl-pdlc-v2-prd.md` (fully superseded) and `-sad.md` (orchestration superseded, Arbiter execution-plane sections still canonical); companion refs incl. the 2026-05-20 critiques (Temporal deferral "explicitly overridden"). **Does NOT list the 2026-05-30 migration plan or the learning-layer preservation doc.**
- **project_arbiter_integration.md** (1319 bytes, 2026-04-01) — Establishes arbiter as centralized LLM/execution plane via gRPC; three engine types `arbiter-api` / `arbiter-agentic` / `arbiter-cli`; full replacement, no fallback. Conceptually aligned with the 2026-05-30 pivot but predates it and is scoped to "LLM clients," not "the whole orchestrator moves to arbiter."
- **pdlc_pipeline_engine.md** (2203 bytes, 2026-03-25) — LEGACY status snapshot. Describes the original in-JVM 12-role engine with role **PM** (not PO), `PlaybookRegistry`, and a TODO list. Largely superseded by the orchestrator pivot; useful only as history.
- **project_conversational_spark_model.md** (2454 bytes, 2026-05-15) — `ConversationSession` GATHERING→PROPOSING→ACTIVE→COMPLETED, `<<<START>>>` marker, `TelegramConversationAdapter`. Superseded by the 2026-05-25 orchestrator (Telegram becomes a thin signaler; markers replaced by persona tool calls), but documents the legacy being removed.
- **project_multiple_pipelines.md** (862 bytes, 2026-03-29) — Pipeline-agnostic UI guidance. Still valid.
- **project_database_migration.md** (841 bytes, 2026-04-17) — Firestore→MongoDB (Hetzner) migration UNFINISHED; do not add new Firestore collections. Still valid and important.
- **project_voice_provider_recommendations.md** (1518 bytes, 2026-05-19) — Deepgram STT + ElevenLabs TTS. Still valid (ratified by the pivot).

### Memory files that NEED UPDATING (do this when resuming)

These are the corrections owed per `00-session-decisions.md` §"Corrections owed" item 3, plus the gaps surfaced above:

1. **`project_cloud_agent_orchestrator.md` — UPDATE (highest priority).** It still places the orchestrator + persona/skill registries + `PipelineWorkflow` inside tacticl-core (Java). Per the 2026-05-30 "Option B" decision the orchestrator + PDLC pipeline **engine moves into `cidadel-ai-arbiter` (TypeScript/Node)**; tacticl-core stays a full product backend (channels, auth, OAuth, billing, REST projections), reading arbiter-written Mongo projections. `PersonaRouter` folds into the workflow as a pure imported function. Voice WS is the only direct browser→arbiter path. Add references to the 2026-05-30 plan and the learning-layer preservation doc (paths in Part B). Cross-reference `00-session-decisions.md` decisions #1-#12.
2. **`reference_cloud_agent_orchestrator_docs.md` — UPDATE.** Add `2026-05-30-orchestrator-migrate-to-arbiter.md` (the migration plan) and `learning-layer-and-codegen-prompts-preservation.md` to the canonical list, and note that execution-plane / orchestrator-location details in the 2026-05-25 PRD/SAD are now revised by the 2026-05-30 set.
3. **`pdlc_pipeline_engine.md` — UPDATE or mark superseded.** Still presents the in-JVM engine and role **PM** as current. Add a banner: engine moves to arbiter; PM→PO; legacy in-JVM orchestrator deleted.
4. **`MEMORY.md` — UPDATE.** Add a top-level entry for the 2026-05-30 orchestrator→arbiter migration (Option B) with the plan + preservation-doc paths, so the index surfaces the latest pivot ahead of the 2026-05-25 entry.

No memory file currently records the 2026-05-30 "Option B" decision (engine → arbiter/TypeScript). That gap is the main risk for a fresh session drifting back to the "build it all in tacticl-core Java" mental model.

---

## Part B — Canonical docs in tacticl-core (read for the migration)

Repo root: `/Users/cuztomizer/Documents/GitHub/tacticl-core`

### Migration plan (the 2026-05-30 doc — START HERE for this migration)

- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/superpowers/plans/2026-05-30-orchestrator-migrate-to-arbiter.md` — **CURRENT.** The orchestrator→arbiter migration plan (55860 bytes, 794 lines, modified 2026-05-30 13:38). This is the plan the migration executes. (Note exact filename: `...-migrate-to-arbiter.md`, NOT `...-arbiter-migration.md`.) Verified header: `Status: Active — supersedes the orchestrator-in-tacticl-core build path from 2026-05-25-cloud-agent-orchestrator.md`; `Decision: Option B (orchestrator engine moves to Arbiter; tacticl-core Java stays as product shell). Option C ... deferred but explicitly preserved (§10).` §1 explains the pivot (LLM calls must go through Arbiter not bypass it; Node is where AI work lives; Java orchestrator would be a thin gRPC shell; orchestrator is platform-level). §2 has Before/After module trees — the "After" tacticl-core keeps `service-conversation` (thin REST/WS adapter → Arbiter gRPC), `service-sparks`, `service-telegram`, `service-pipeline` (reads projections), thin `business-*`/`data-*`, `client-ai-arbiter` (extended), a NEW `client-cloud-orchestrator` (gRPC client for orchestrator RPCs), and `application-api` WITHOUT Temporal; and DELETES (moved to Arbiter): `business-cloud-orchestrator`, `data-cloud-orchestrator`, `business-voice`, `client-deepgram`, `client-elevenlabs`, `service-cloud-orchestrator`, the Temporal Java bootstrap. Per `00-session-decisions.md` §"Corrections owed" #1, its §3 still needs editing to replace "product shell" language with "full product backend" and to ADD a "Preserve & integrate (arbiter learning layer)" subsection + a DO-NOT-DROP list for Mongo `agent_knowledge` and the Qdrant collection (the preservation doc §4 lists these exact action items as still unchecked). This plan lives ONLY in tacticl-core (no copy in the arbiter repo — see Part C).

### PRD / SAD / 2026-05-25 plan

- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/superpowers/specs/2026-05-25-cloud-agent-orchestrator-prd.md` — **CURRENT for product requirements; orchestrator-location/execution-plane details now revised by the 2026-05-30 plan.** (44917 bytes, 2026-05-30 10:59.) Frontmatter `type: product-requirements, status: draft, date: 2026-05-25, author: Gabriel Jimenez`. Personas, voice plane, sphere UI, spark model (0..N sparks/session), confirmation tiers, Temporal-deferral override.
- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/superpowers/specs/2026-05-25-cloud-agent-orchestrator-sad.md` — **CURRENT for system architecture; execution-plane/location revised by 2026-05-30.** (79080 bytes, 2026-05-30 11:01.) `type: engineering-spec`. Temporal workflows/activities, `PersonaRegistry`/`SkillRegistry`, Mongo schema (§3.7 projection pattern referenced by decisions #6), voice bridges. The open question in `00-session-decisions.md` (persona/skill registry scoping: shared product-tagged vs per-product namespaces) is NOT yet resolved in this SAD.
- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/superpowers/plans/2026-05-25-cloud-agent-orchestrator.md` — **PARTIALLY SUPERSEDED** by the 2026-05-30 plan (execution-plane / engine-location phases). (36634 bytes, 2026-05-29 00:30.) Conversation/voice/persona/sphere/spark-model phases largely still inform the work. Goal line still says "Replace the dual ConversationService + PdlcV2Service brains with a single Temporal-backed CloudAgentSessionWorkflow ... PDLC execution stays in Arbiter unchanged."

### Learning-layer preservation doc (IT EXISTS — read it)

- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/architecture/learning-layer-and-codegen-prompts-preservation.md` — **CURRENT (NEW, 2026-05-30 15:23, 8998 bytes, 72 lines — FULL CONTENT VERIFIED).** The dedicated learning-layer + codegen-prompts preservation doc cited in `00-session-decisions.md` §12. Verdict: the learning layer is NOT at risk (committed in arbiter since April 2026; value is data in Mongo + Qdrant, not source). §1 is a verified inventory of the arbiter-side machinery with exact paths and first-commit dates: `KnowledgeLoader` (`packages/server/src/shell/knowledge-loader.ts`, `loadForAgent(product, agentType)` → `{authored, learned}`), `KnowledgeIndexer` (`shell/knowledge-indexer.ts`), `LearningProposer` (`retro/learning-proposer.ts`, writes Mongo `agent_knowledge` status `proposed`), `RetroAgent` (`retro/retro-agent.ts` + `retro-boot.md`), `qdrant-client.ts`, `embedding-client.ts`, registry (`shell/registry-cache.ts`/`registry-client.ts` — note: "there is no `pipeline/role-prompts.ts`"), `pipeline-tracker.ts`, `scripts/index-knowledge.ts`, `scripts/migrate-knowledge-firestore-to-mongo.ts`, and two knowledge tests. §2 = accumulated value lives in Mongo `agent_knowledge` + Qdrant (changing embedding model forces a Qdrant recreate → re-run `index-knowledge.ts`, no data loss). §4 = the ONE real risk (rebuilt `PipelineWorkflow` forgets to call `KnowledgeLoader` + `RetroAgent`/`LearningProposer` → capability silently goes dark) and the unchecked action items (these are the items `00-session-decisions.md` §"Corrections owed" #1 wants folded into the migration plan §3). Read in full before touching any learning/knowledge code.

### Architecture docs (`docs/architecture/*`) — verified directory contents

`ls` / `find -maxdepth 1` confirm **exactly these** files in `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/architecture/` (plus a `critiques/` subdir). There is **no** `pdlc-pipeline-arbiter-migration.md` and **no** `temporal-workflow-orchestration-architecture.md` in this repo.

- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/architecture/learning-layer-and-codegen-prompts-preservation.md` — see above (CURRENT).
- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/architecture/tacticl-cidadel-arbiter-architecture.drawio` and `.drawio.png` — **CURRENT (NEW, 2026-05-30).** The tacticl↔cidadel↔arbiter architecture diagram. Per `00-session-decisions.md` §"Corrections owed" #4, treat the `.drawio` as the SOURCE OF TRUTH if regenerating; it already reflects "one Orchestrator + child PipelineWorkflow" and "full product, any size."
- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/architecture/ecosystem-arbiter-architecture.drawio` and `.drawio.png` — **CURRENT (NEW, 2026-05-30).** The broader ecosystem (all products → shared arbiter) diagram. Also source-of-truth `.drawio`.
- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/architecture/pdlc-pipeline-architecture.md` — **SUPERSEDED for orchestration (2026-03-25).** Original engine: 12 roles (status line: "Wave 1–3 complete; Wave 4 role execution in progress"), modules `business-agent` / `data-social` / `service-agent`. Read only as history of the in-JVM engine being removed.
- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/architecture/cloud-orchestrator-architecture.md` — **PARTIALLY SUPERSEDED (2026-03-09).** Legacy in-JVM `CloudOrchestratorService` description. Skill-executor role survives (wrapped as an activity); agent-loop-as-entrypoint replaced by the Temporal orchestrator (and now relocating to arbiter).
- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/architecture/device-agent-architecture.md` — **CURRENT (forward-looking, 2026-03-10).** Device daemon: WebSocket spark dispatch, tactic decomposition, 9 command types, Claude Code device engine. Device Agent Orchestrator is a future sibling, out of scope for this migration.
- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/architecture/critiques/` — subdir with the 2026-05-20 critique set: `2026-05-20-ai-agent-critique.md`, `-incremental-delivery-critique.md`, `-ops-critique.md`, `-platform-sre-critique.md`, `-security-critique.md`, `-synthesis.md`, `-workflow-temporal-critique.md`. These recommended DEFERRING Temporal — **explicitly overridden** by the 2026-05-25 pivot. Read only for context on the trade-offs that were knowingly accepted.

### Older referenced docs (context only, superseded for this migration)

- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/superpowers/specs/2026-04-11-tacticl-pdlc-v2-prd.md` — **fully superseded** (per reference_cloud_agent_orchestrator_docs.md). Has uncommitted local modifications (`git status: M`).
- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/superpowers/specs/2026-04-11-tacticl-pdlc-v2-sad.md` — **orchestration sections superseded; Arbiter execution-plane sections (workspace assembly, gRPC protocol, 4-layer knowledge, Mongo pipeline schema) remain canonical** and are referenced from the new SAD. Also `git status: M`.
- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/superpowers/plans/2026-05-15-telegram-conversational-spark-to-pdlc.md` (+ its PRD/design) — **SUPERSEDED** by the orchestrator pivot (Telegram becomes a thin signaler; marker protocol replaced by persona tool calls). Read only for the legacy `ConversationService`/`TelegramConversationAdapter` behaviour being removed.
- `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/superpowers/specs/2026-04-01-tacticl-arbiter-grpc-integration-design.md` and `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/superpowers/plans/2026-04-01-tacticl-arbiter-grpc-integration.md` — **CONTEXT.** The original tacticl↔arbiter gRPC integration design; foundational for the deeper "engine → arbiter" move. (Spec path inferred from memory file `project_arbiter_integration.md`; plan filename verified present in the plans dir.)

---

## Part C — Arbiter-side docs (cidadel-ai-arbiter)

Repo root: `/Users/cuztomizer/Documents/GitHub/cidadel-ai-arbiter` (TypeScript/Node; packages under `packages/`, scripts under `scripts/`). Its docs tree is small and was FULLY enumerated via `find -type f`:

- The arbiter `docs/` tree contains **exactly ONE file**: `/Users/cuztomizer/Documents/GitHub/cidadel-ai-arbiter/docs/superpowers/plans/2026-04-17-arbiter-rewrite.md`. (No top-level `.md`, no `specs/`, no migration-plan copy.) An earlier draft asserted files named `tacticl-pipeline-arbiter-migration.md` and a `2026-05-30-orchestrator-migrate-to-arbiter.md` copy there — BOTH were GUESSES and are WRONG; neither exists.
- `/Users/cuztomizer/Documents/GitHub/cidadel-ai-arbiter/docs/superpowers/plans/2026-04-17-arbiter-rewrite.md` — **CURRENT (implementation plan, verified header).** Title: "Arbiter Rewrite: Persistent NDJSON Agent Execution Implementation Plan." Goal (verbatim): transform cidadel-ai-arbiter from a polling-based one-shot executor into a persistent NDJSON session manager with agent-driven push reporting, role identity loading, GitHub integration, and watchdog TTL enforcement. Architecture: each agent container runs `sleep infinity`; arbiter holds one long-lived `docker exec -i <container> claude --output-format stream-json` subprocess per agent (NdjsonSession); agents push status via `report.sh` → `/api/notify` or `/api/ask` with per-agent bearer tokens; **"arbiter is fully product-agnostic"** (products send role identities + playbook config in the gRPC request). Tech: TypeScript 5, Node 22, Dockerode, Express, `@grpc/grpc-js`, Vitest, Claude Code CLI, `gh`. Its header explicitly says "Repos touched: cidadel-ai-arbiter only. tacticl-core contract changes are in a separate plan." This is the foundational arbiter-side execution model the 2026-05-30 "Option B" migration builds on. (Caveat for decision #10: this plan's "fully product-agnostic" framing predates and is REFINED by `00-session-decisions.md` #10 — the engine is product-AWARE via data/scoping, not branchy code. Treat #10 as the newer word.)
- The arbiter repo's `CLAUDE.md` (`/Users/cuztomizer/Documents/GitHub/cidadel-ai-arbiter/CLAUDE.md`, 4229 bytes) is the operative orientation doc for the arbiter side — read it for embedding providers, Qdrant knowledge store, Vault provider secrets, and the `packages/` layout. The learning-layer source lives under `packages/server/src/shell/` and `packages/server/src/retro/` (exact paths in the tacticl-core preservation doc §1). The arbiter side is where the orchestrator + pipeline engine + learning layer (`agent_knowledge` Mongo + Qdrant, `KnowledgeLoader`, `RetroAgent`/`LearningProposer`) live (per decisions #9-#12).
- NOTE: the 2026-05-30 migration plan itself lives ONLY in tacticl-core (`docs/superpowers/plans/2026-05-30-orchestrator-migrate-to-arbiter.md`), even though it is a multi-repo plan that mutates both repos. There is no arbiter-repo copy.

---

## Suggested read order for a fresh session

1. `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/handover/2026-05-30-orchestrator-migration/00-session-decisions.md` — the locked decisions (Option B, one orchestrator + child PipelineWorkflow, voice-WS exception, product-aware-via-data, learning layer not at risk but must be re-wired).
2. `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/superpowers/plans/2026-05-30-orchestrator-migrate-to-arbiter.md` — the migration plan (apply its §3 corrections early).
3. `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/architecture/learning-layer-and-codegen-prompts-preservation.md` — the DO-NOT-DROP / re-wire spec for `agent_knowledge` + Qdrant.
4. The two `.drawio` diagrams in `/Users/cuztomizer/Documents/GitHub/tacticl-core/docs/architecture/` (source of truth for the target shape).
5. `/Users/cuztomizer/Documents/GitHub/cidadel-ai-arbiter/docs/superpowers/plans/2026-04-17-arbiter-rewrite.md` + the arbiter `CLAUDE.md` — the arbiter-side multi-tenant engine model and orientation.
6. The 2026-05-25 PRD/SAD — for persona/voice/spark-model/Temporal detail (treat orchestrator-location + execution-plane sections as revised by step 2).
7. The Part A memory files — but FIRST apply the 4 updates in §"Memory files that NEED UPDATING"; until then, treat their tacticl-core-hosts-the-engine framing as stale.
